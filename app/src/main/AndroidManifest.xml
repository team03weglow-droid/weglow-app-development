<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Supabase / OAuth network requests -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required for WeGlow camera / skin scan -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera is optional so devices without a camera can still install the app -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weglow">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Weglow"
            android:windowSoftInputMode="adjustResize">

            <!-- Normal app launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Supabase OAuth callback.

                Google/Facebook
                    ↓
                Supabase
                    ↓
                weglow://login-callback
                    ↓
                MainActivity
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="weglow"
                    android:host="login-callback" />
            </intent-filter>

        </activity>

        <!--
            Phase 6 follow-up: narrow FileProvider used ONLY to hand the system
            camera a single private cache file for a profile-picture capture.
            Not exported; URI access is granted per-launch and only to the
            camera app that is started. No broad/external storage is involved.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is denied by default. The chat backend is a hosted Supabase
  Edge Function served over HTTPS, so no per-host cleartext exceptions are
  needed.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
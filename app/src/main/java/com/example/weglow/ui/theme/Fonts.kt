package com.example.weglow.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.weglow.R

val HurricaneFont = FontFamily(Font(R.font.hurricane_regular))
val JungeFont = FontFamily(Font(R.font.junge_regular))

// Native sans-serif stays crisp at small sizes and supports real UI font weights.
// Junge is reserved for editorial headings; Hurricane remains the brand wordmark.
val UiFont = FontFamily.SansSerif
